const greeting = "Hello World";

const answer = 'Forty Two';
// this interpilates when it is evaluated the first time
// but because this is a const and not a var once interpilated
// it doesn't change!
const html = `
  <div>
    ${Math.random()}
  </div>
`;
