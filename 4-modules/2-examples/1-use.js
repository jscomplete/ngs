const api = require('./1-object');

console.log(api.language, api.direction, api.encoding);

// when converted to ES6:
// import { language, direction, encoding } from './1-object';

// console.log(language, direction, encoding);